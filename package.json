{
  "name": "thinknode-application",
  "description": "application created by thinknode",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run eslint",
    "start": "npm run compile && node www/index.js",
    "compile": "babel src -d App -D",
    "watch-compile": "npm run compile -- --watch",
    "eslint": "eslint src"
  },
  "babel": {
    "presets": [
      ["env", {"targets": {"node": true}, "loose": true}]
    ],
    "plugins": [
      "transform-runtime"
    ]
  },
  "dependencies": {
    "babel-runtime": "6.22.0",
    "thinknode": "4.x.x"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-core": "^6.22.1",
    "babel-eslint": "^7.1.1",
    "babel-plugin-transform-runtime": "^6.22.0",
    "babel-preset-env": "^1.1.8",
    "chokidar": "^1.6.1",
    "debug": "^2.2.0",
    "eslint": "^3.15.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "muk": "^0.5.3"
  }
}
